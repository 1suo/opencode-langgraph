Ranked by actual hours lost today:
1. Subagent session kills (biggest structural one). The 5-minute inactivity timeout has killed activations in almost every run (c2's inspect, c3's implement a4, the meta-runs' a1/a2). Each kill discards that activation's partial work and the scheduler retries from scratch — a killed implement activation can burn 20+ minutes of duplicated work. The API connectivity flakiness ("Cannot connect to API", c2 lost two implement attempts) stacks on top. This is plugin/runtime-level, not graph-design.
2. The sequential constraint itself. 10 corrections × 60–90 min healthy-run time = inherently a full day. True parallelism is limited: every correction edits TODO-process-designer.md bookkeeping, so they serialize on that file even if code areas differ.
3. My wedge on c3 (~40 min, self-inflicted). I paused a run mid-flight and the scheduler never recovered. Lesson is now protocol: never pause/poke a run whose counters are merely slow; only act after the 30-min ceiling has demonstrably fired.
4. The headless detour (~1 h). c1's first implementation was abandoned mid-work when we switched mechanisms and had to be redone by the tool-started run.
5. Per-run ceremony overhead. Every run spends its first 1–2 calls re-inspecting the repo even when the verdict on record fully determines the design. For corrections like c4 ("add one test + bookkeeping"), a full inspect→synthesize→implement→verify run is arguably overkill — grouping c4-sized corrections into one run would cut total ceremony.
6. Merge friction from the dirty primary checkout. Your uncommitted TODO-process-designer.md edits aborted c3's first merge and forced the remainder run to plan around them. Committing or stashing those removes a recurring failure mode from every remaining correction.
Fixes within reach right now: #3 (done — protocol adopted), #6 (you, one commit), #5 (I can batch c4 into c5's scope when we get there). #1 is the one that needs plugin-level attention — the kills are recoverable now, but they're still the largest steady tax.
